void writeString(char * a){
	return ;
}
int readInteger(){
	return 1;
}
void writeReal(double a){
	return;
}
void main ()
{
	int n, k, i, seed;
	double sum;
	writeString("Give n: ");
	n = readInteger();
//	writeString("Give k: ");
//	k = readInteger();
//	for (i = 0, sum = 0.0, seed = 65; i < k; i++)
//		sum += (double) (seed = (seed * 137 + 221 + i) % n);
	if (5 > 0) {
		writeString("Mean: ");
		writeReal(sum /  i);
		writeString("\n");
	}
}
